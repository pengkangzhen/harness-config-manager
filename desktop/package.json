{
  "name": "halter-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Harness Config Manager desktop app (Tauri + halter sidecar)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "npm run build:sidecar && tauri build",
    "build:sidecar": "cd .. && ./scripts/build_sidecar.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0"
  }
}
