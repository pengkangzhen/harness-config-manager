{
  "name": "halter-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Harness Config Manager desktop app (Tauri + halter sidecar)",
  "scripts": {
    "tauri": "tauri",
    "dev": "npm run build:sidecar && tauri dev",
    "build": "npm run build:sidecar && tauri build",
    "build:sidecar": "cd .. && sh scripts/build_sidecar.sh",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@tauri-apps/cli": "^2.9.0"
  }
}
